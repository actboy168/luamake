# luamake

## Build

```bash
git clone https://github.com/actboy168/luamake
cd luamake
git submodule update --init
```

msvc
* compile/install.bat

mingw / macos / linux

* Install ninja
* compile/install.sh
